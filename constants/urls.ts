let serverURL: string;
// if (!process.env.NODE_ENV || process.env.NODE_ENV === "development") {
//   serverURL = "http://localhost:5000/api/v0.01";
// } else {
//   serverURL = serverURL = "https://tulime-backend.onrender.com/api/v0.01";
// }
serverURL = serverURL = "https://tulime-backend.onrender.com/api/v0.01";

export { serverURL };
